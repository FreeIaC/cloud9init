!/usr/bin/env bash -e
workdir=$(pwd)
app=$(basename $0)

test -f ${workdir}/_${app}.done && { echo "== log: workdir: $workdir; app: $app; we DID it, exiting" ; exit 0 ; }

echo "== log: bash aliases: $0"

touch ${workdir}/_${app}.done
